import React from "react";
import Chat from "./pages/Chat";

const App: React.FC = () => (
  <div className="min-h-screen w-full bg-gradient-to-br from-blush via-pink to-lavender flex items-center justify-center">
    <Chat />
  </div>
);

export default App;
